/** Formulario validado con `esquemaRegistro` de @appweb/core/tipos. */

export const metadata = { title: "Crear cuenta" };

export default function PaginaRegistro() {
  return (
    <main>
      <h1>Crear cuenta</h1>
    </main>
  );
}
