/** Formulario validado con `esquemaIngreso` de @appweb/core/tipos. */

export const metadata = { title: "Ingresar" };

export default function PaginaIngresar() {
  return (
    <main>
      <h1>Ingresar</h1>
    </main>
  );
}
