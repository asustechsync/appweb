/** CLASE A — portada. Etiqueta `portada`. Presupuesto: 20-40 ms. */

export default function PaginaPortada() {
  return (
    <main>
      <h1>Tienda</h1>
      {/* Hero, categorias destacadas y productos destacados */}
    </main>
  );
}
